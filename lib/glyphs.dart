/// Optically centered icon glyphs built from Font Awesome SVG sources.
///
/// Each glyph has its minimum-enclosing-circle (MEC) center pre-baked at
/// the viewBox center, so Flutter's `Icon` renders it visually centered
/// without any wrapping widget.
library;

export 'src/glyphs.g.dart';
